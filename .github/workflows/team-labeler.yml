
name: Team Label
on:
  workflow_dispatch:
  pull_request_target:
    types: [opened]
jobs:
  call_reusable_workflow:
    name: Team Labeler
    uses: jonathan-kline/hello-world/.github/workflows/Team-Label.yml@main
    with:
      teams-repo: hello-world
      configuration-path: .github/teams.yml
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
