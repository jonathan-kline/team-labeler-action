name: Release Drafter

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - main
jobs:
  Update_Release_Draft:
    uses: jonathan-kline/hello-world/.github/workflows/Release-Drafter.yml@main
    with:
      config-path: .github/release-drafter.yml
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

